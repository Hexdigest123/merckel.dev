# Database (required for tools platform)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/merckel

# Email service (optional — contact form works in local mode without it)
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# GitHub API (optional — open source section uses fallback data without it)
GITHUB_TOKEN=
